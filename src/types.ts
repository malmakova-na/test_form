export interface FormValues {
  title: string;
  description: string;
  price: number;
  category: string;
  phone: string;
  files?: FileList;
  video: string;
  address: string;
  connect: string;
  condition: string;
}
